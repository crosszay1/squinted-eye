[project]
name = "squinted_eye"
version = "0.1.0"
description = "The OSINT tool for real time information and target monitoring"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
]
